<% posts.each do |post| %>

<div class = "fleet">
	<ul>
		<li><%= link_to "#{post.user.username}", posts_path %></li>
		<li> <%= link_to "#{post.flight.airline.name} Flight #{post.flight.flight_number}", flight_path(post.flight.id) %></li>
		<li><%=post.text%></li>

	</ul>
</div>
<% end %>

